[project]
name = "wardenpy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argon2-cffi>=23.1.0",
    "cryptography>=43.0.3",
]

[dependency-groups]
dev = [
    "pyright>=1.1.389",
    "pytest>=8.3.3",
]
